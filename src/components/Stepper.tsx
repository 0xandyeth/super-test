import React from 'react';
import styled from 'styled-components';
interface StepperProps{

};
const Form:React.FC<StepperProps>=()=>{
  return(
    <Container>
    </Container>  
  )
}

const Container = styled.div`
`

export default Form;